<table class="listing" cellspacing="0">
  <tr class="header">
    <th>Name</th>
    <th>Affiliation</th>
    <th>Disabled at</th>
  </tr>
  <% @contacts.each do |contact| -%>
  <tr class="<%= cycle("list-line-odd", "list-line-even") -%>">
    <td><%= link_to h(contact.full_name), contact -%></td>
    <td><%= contact.affiliation -%></td>
    <% if not contact.enabled? -%>
    	<td><%= nice_date contact.disabled_at -%></td>
    <% else -%>
		<td></td>
	<% end -%>
  </tr>
  <% end -%>
</table>

<%= will_paginate @contacts, :previous_label => '&#8249;', :next_label => '&#8250;' %>
<% if @total_contacts -%>
  <div style="margin: 6px 0; font-style: italic; color: #333; text-align:center;"><%=@total_contacts %> <%= @total_contacts == 1 ? "contact" : "contacts" %> found.</div>
<% end -%>
